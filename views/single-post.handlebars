{{#with post}}
    <section class='post'>
        <div class='post-header'>
            <h2 class='post-title'>{{title}}</h2>

            <span class='post-author'><a href='/users/{{user.id}}'>{{user.username}}</a></span>

            <span>{{format_date created_at}}</span>
        </div>

        <div class='post-text'>
            {{text}}
        </div>

        {{#if belongsToUser}}
            <div class="post-action-links">
                <a class="link-btn" href="/edit/{{id}}">Edit</a>

                <button class="link-btn" id="delete-btn" type="button">Delete</button>
            </div>
        {{/if}}
    </section>
{{/with}}

{{#if loggedIn}}
    <form id="comment-form" class="comment-form flex flex-col">
        <h3 class='form-title'>Leave a comment?</h3>

        <textarea id='comment_text' placeholder='Comment text...'></textarea>

        <button type="submit">Submit</button>
    </form>

    <script src="/js/comment.js"></script>
{{/if}}

{{#with post}}
    <section class='comment-section'>
        {{#if comments.length}}
            {{> comments comments}}
        {{else}}
            <p>There are no comments on this post.</p>
        {{/if}}
    </section>
{{/with}}